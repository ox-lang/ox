package io.oxlang

/*
object Reader {
  @JvmStatic public fun <T> read(stream: IOReader, streamIdentifier: T): ReadObject<T> {
    // yo dawg I heard u leik streams
    val iter = Scanner.scan(PushbackReader(stream), streamIdentifier)
  }

  @JvmStatic public fun <T> readStr(buff: String, streamIdentifier: T): ReadObject<T> {
    return read<T>(StringReader(buff), streamIdentifier)
  }

  @JvmStatic public fun main(args: Array<String>) {
    for ((index, arg) in args.withIndex()) {
      println(readStr(arg, String.format("Arg %d", index)))
    }
  }

}
*/
